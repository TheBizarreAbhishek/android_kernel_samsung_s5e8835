name: Build Kernels

on:
    workflow_dispatch:
        inputs:
            prerelease:
                description: "Mark the GitHub release as a prerelease"
                required: false
                default: false
                type: boolean
            notifications:
                description: "Telegram: post release messages"
                required: false
                default: false
                type: boolean
            enable_bbrv3:
                description: "Enable BBRv3 TCP congestion control"
                required: false
                default: true
                type: boolean
            enable_zeromount:
                description: "Enable ZeroMount path redirection (all variants)"
                required: false
                default: true
                type: boolean

permissions:
    contents: write
    actions: write

concurrency:
    group: build-kernels-${{ github.ref }}
    cancel-in-progress: false

env:
    TOOLCHAIN_DIR: /home/runner/toolchain/clang-r450784d

defaults:
    run:
        shell: bash

jobs:
    build:
        name: Build Bizarre Kernel (${{ matrix.variant }})
        runs-on: ubuntu-latest
        strategy:
            fail-fast: false
            matrix:
                include:
                    - variant: "clean"
                      kernelsu: "none"
                      susfs: "false"
                      permissive: "false"
                      zeromount: "true"
                    - variant: "permissive"
                      kernelsu: "none"
                      susfs: "false"
                      permissive: "true"
                      zeromount: "true"
                    - variant: "ksu"
                      kernelsu: "ksu"
                      susfs: "false"
                      permissive: "false"
                      zeromount: "true"
                    - variant: "ksunext"
                      kernelsu: "ksunext"
                      susfs: "false"
                      permissive: "false"
                      zeromount: "true"
                    - variant: "ksu-susfs"
                      kernelsu: "ksu"
                      susfs: "true"
                      permissive: "false"
                      zeromount: "true"
                    - variant: "ksunext-susfs"
                      kernelsu: "ksunext"
                      susfs: "true"
                      permissive: "false"
                      zeromount: "true"
        env:
            BUILD_LOCALVERSION: -Bizarre-${{ matrix.variant }}

        steps:
            - name: Free disk space
              uses: endersonmenezes/free-disk-space@v3
              with:
                  remove_android: true
                  remove_dotnet: true
                  remove_haskell: true
                  remove_tool_cache: true
                  remove_swap: true

            - name: Checkout Kernel Source
              uses: actions/checkout@v4

            - name: Set date and start time
              run: |
                  echo "DATE=$(date +'%Y-%m-%d')" >> "$GITHUB_ENV"
                  echo "START_TIME=$(date +%s)" >> "$GITHUB_ENV"
                  echo "📅 Build date: $(date +'%Y-%m-%d %H:%M:%S UTC')"

            - name: Install dependencies
              env:
                  DEBIAN_FRONTEND: noninteractive
              run: |
                  echo "📦 Updating package lists..."
                  sudo apt-get update -qq
                  echo "📦 Installing build dependencies..."
                  sudo apt-get install -y -qq \
                    git device-tree-compiler lz4 xz-utils zlib1g-dev openjdk-17-jdk gcc g++ python3 python-is-python3 \
                    p7zip-full android-sdk-libsparse-utils erofs-utils default-jdk gnupg flex bison gperf build-essential \
                    zip curl libc6-dev libncurses-dev libx11-dev libreadline-dev libgl1 libgl1-mesa-dev bc grep tofrodos \
                    python3-markdown libxml2-utils xsltproc libtinfo6 make repo cpio kmod openssl libelf-dev pahole \
                    libssl-dev libarchive-tools zstd rsync --fix-missing
                  echo "✅ All dependencies installed"

            - name: Setup Clang Toolchain
              run: |
                  mkdir -p "${{ env.TOOLCHAIN_DIR }}"
                  echo "⬇️  Cloning Bizarre Clang r450784d toolchain..."
                  git clone --depth=1 https://github.com/TheBizarreAbhishek/clang-r450784d.git "${{ env.TOOLCHAIN_DIR }}"
                  echo "${{ env.TOOLCHAIN_DIR }}/bin" >> $GITHUB_PATH
                  echo "✅ Toolchain ready"

            - name: Fetch AnyKernel3
              run: |
                  echo "⬇️  Cloning TheBizarreAbhishek/AnyKernel3..."
                  git clone --depth=1 --branch bizarre https://github.com/TheBizarreAbhishek/AnyKernel3.git "$HOME/AnyKernel3"
                  echo "✅ AnyKernel3 ready"

            - name: Import KernelSU
              if: matrix.kernelsu != 'none'
              run: |
                  set -euo pipefail
                  cd "$GITHUB_WORKSPACE"
                  if [[ "${{ matrix.kernelsu }}" == "ksunext" ]]; then
                    if [[ "${{ matrix.susfs }}" == "true" ]]; then
                      echo "🔧 Integrating KernelSU Next (pershoot dev-susfs)..."
                      curl -LSs "https://raw.githubusercontent.com/pershoot/KernelSU-Next/next-susfs/kernel/setup.sh" | bash -s dev-susfs
                      echo "✅ KernelSU-Next (pershoot dev-susfs) driver ready"
                    else
                      echo "🔧 Integrating KernelSU Next (rifsxd next)..."
                      curl -LSs "https://raw.githubusercontent.com/rifsxd/KernelSU-Next/next/kernel/setup.sh" | bash -s next
                      echo "✅ KernelSU-Next driver ready"
                    fi
                  else
                    echo "🔧 Integrating KernelSU (tiann - stable)..."
                    curl -LSs "https://raw.githubusercontent.com/tiann/KernelSU/main/kernel/setup.sh" | bash
                    echo "✅ KernelSU driver ready"
                  fi

            - name: Apply SUSFS patches
              if: matrix.susfs == 'true'
              run: |
                  set -euo pipefail
                  cd "$GITHUB_WORKSPACE"

                  echo "🩹 Applying SUSFS kernel patch..."
                  patch -p1 --forward < scripts/patches/kernel/0001-susfs-for-samsung-s5e8835.patch \
                    || echo "  ⚠️  Some hunks in SUSFS patch were skipped (expected for partial apply)"

                  # Add SUSFS_VERSION info to kernel/Makefile
                  if ! grep -q "SUSFS_VERSION" kernel/Makefile 2>/dev/null; then
                    echo "🔧 Adding SUSFS_VERSION to kernel/Makefile..."
                    echo "" >> kernel/Makefile
                    echo "## For susfs stuff ##" >> kernel/Makefile
                    echo "ifeq (\$(shell test -e \$(srctree)/fs/susfs.c; echo \$\$\?),0)" >> kernel/Makefile
                    echo "\$(eval SUSFS_VERSION=\$(shell cat \$(srctree)/include/linux/susfs.h | grep -E '^#define SUSFS_VERSION' | cut -d' ' -f3 | sed 's/\"//g'))" >> kernel/Makefile
                    echo "\$(info )" >> kernel/Makefile
                    echo "\$(info -- SUSFS_VERSION: \$(SUSFS_VERSION))" >> kernel/Makefile
                    echo "else" >> kernel/Makefile
                    echo "\$(info -- SUSFS: susfs.c not found, SUSFS not integrated)" >> kernel/Makefile
                    echo "endif" >> kernel/Makefile
                  fi

                  # Apply KSU driver SUSFS hook patch (tiann only)
                  if [[ "${{ matrix.kernelsu }}" == "ksu" ]]; then
                    echo "🩹 Applying KSU tiann SUSFS hook patch..."
                    patch -p1 -d drivers/kernelsu \
                      < scripts/patches/ksu/tiann/0001-susfs-hook-tiann.patch \
                      || echo "  ⚠️  Some KSU hook hunks skipped (may already be applied)"
                  fi

                  find . -name '*.rej' -delete 2>/dev/null || true
                  echo "✅ SUSFS patches applied cleanly"

            - name: Add NTSYNC
              run: |
                  set -euo pipefail
                  cd "$GITHUB_WORKSPACE"
                  echo "🩹 Applying NTSYNC..."
                  # NTSYNC is always applied from WildKernels patches repository
                  git clone --depth=1 https://github.com/WildKernels/kernel_patches.git "$HOME/kernel_patches"
                  patch -p1 < "$HOME/kernel_patches/common/ntsync/ntsync_base.patch" || true
                  patch -p1 < "$HOME/kernel_patches/common/ntsync/ntsync_compat_android13-5.15.patch" || true
                  echo "✅ NTSYNC applied"

            - name: Add BBRv3
              if: github.event.inputs.enable_bbrv3 != 'false'
              run: |
                  set -euo pipefail
                  cd "$GITHUB_WORKSPACE"
                  echo "⚙️ Applying BBRv3 from local patch..."
                  patch -p1 < scripts/patches/kernel/0002-bbrv3-android13-5.15.patch || true
                  echo "✅ BBRv3 applied"

            - name: Apply Zeromount patch
              if: matrix.zeromount == 'true' && github.event.inputs.enable_zeromount != 'false'
              run: |
                  set -euo pipefail
                  ZB="$HOME/Super-Builders"
                  ZM_PATCH="$ZB/android13-5.15/WildKSU/patches/60_zeromount-android13-5.15.patch"

                  echo "⬇️  Cloning Super-Builders..."
                  rm -rf "$ZB"
                  git clone --depth=1 https://github.com/jimsterino98/Super-Builders.git "$ZB"
                  if [[ ! -f "$ZM_PATCH" ]]; then
                    echo "ERROR: Zeromount patch not found: $ZM_PATCH" >&2
                    exit 1
                  fi

                  cd "$GITHUB_WORKSPACE"
                  echo "🩹 Applying Zeromount patch..."
                  cp "$ZM_PATCH" ./60_zeromount-android13-5.15.patch
                  patch -p1 < 60_zeromount-android13-5.15.patch || true
                  rm -f 60_zeromount-android13-5.15.patch

                  # Fix fs/namei.c — inject zeromount.h include if patch hunk failed
                  if ! grep -q 'zeromount.h' fs/namei.c; then
                    echo "🔧 Injecting zeromount.h include into fs/namei.c..."
                    sed -i '/#include "internal.h"/i #ifdef CONFIG_ZEROMOUNT\n#include <linux/zeromount.h>\n#endif' fs/namei.c
                  fi

                  if [[ -f fs/proc/task_mmu.c ]] && ! grep -q 'zeromount_spoof_mmap_metadata' fs/proc/task_mmu.c; then
                    echo "🔧 Fixing fs/proc/task_mmu.c..."
                    _zm_task_mmu=$(mktemp)
                    printf '%s\n' \
                      '#ifdef CONFIG_ZEROMOUNT' \
                      $'\tzeromount_spoof_mmap_metadata(inode, &dev, &ino);' \
                      '#endif' > "$_zm_task_mmu"
                    sed -i "/pgoff = ((loff_t)vma->vm_pgoff) << PAGE_SHIFT;/r $_zm_task_mmu" fs/proc/task_mmu.c
                    rm -f "$_zm_task_mmu"

                    _zm_include=$(mktemp)
                    printf '%s\n' \
                      '#ifdef CONFIG_ZEROMOUNT' \
                      '#include <linux/zeromount.h>' \
                      '#endif' > "$_zm_include"
                    sed -i "/#include <linux\/pagewalk.h>/r $_zm_include" fs/proc/task_mmu.c
                    rm -f "$_zm_include"
                  fi

                  if [[ -f fs/Kconfig.rej ]]; then
                    echo "🔧 Fixing fs/Kconfig..."
                    if ! grep -q '^config ZEROMOUNT$' fs/Kconfig; then
                      perl -0pi -e 's@(config IO_WQ\n\tbool\n)@\1\nconfig ZEROMOUNT\n\tbool "ZeroMount Path Redirection Subsystem"\n\tdefault y\n\thelp\n\t  ZeroMount allows path redirection and virtual file injection\n\t  without mounting filesystems.\n\n@' fs/Kconfig
                    fi
                    rm -f fs/Kconfig.rej
                  fi

                  rm -f fs/namei.c.rej fs/readdir.c.rej fs/stat.c.rej fs/proc/task_mmu.c.rej 2>/dev/null || true
                  echo "✅ Zeromount applied"

            - name: Configure build options
              run: |
                  set -euo pipefail
                  cd "$GITHUB_WORKSPACE"
                  echo "⚙️  Generating custom.config..."
                  touch arch/arm64/configs/custom.config

                  # Base config options
                  ./scripts/config --file arch/arm64/configs/custom.config --enable CONFIG_SLUB_DEBUG
                  ./scripts/config --file arch/arm64/configs/custom.config --disable CONFIG_SCHED_AUTOGROUP

                  if [[ "${{ matrix.permissive }}" == "true" ]]; then
                    echo "⚙️ Enabling Permissive Mode..."
                    ./scripts/config --file arch/arm64/configs/custom.config --disable CONFIG_SECURITY_SELINUX_DEVELOP
                  fi

                  if [[ "${{ matrix.kernelsu }}" != "none" ]]; then
                    echo "⚙️ Enabling KernelSU..."
                    ./scripts/config --file arch/arm64/configs/custom.config --enable CONFIG_KSU
                    ./scripts/config --file arch/arm64/configs/custom.config --disable CONFIG_KSU_KPROBES_HOOK
                  fi

                  if [[ "${{ matrix.susfs }}" == "true" ]]; then
                    echo "⚙️ Enabling SUSFS..."
                    ./scripts/config --file arch/arm64/configs/custom.config \
                      --enable CONFIG_KSU_SUSFS \
                      --enable CONFIG_KSU_SUSFS_SUS_PATH \
                      --enable CONFIG_KSU_SUSFS_SUS_MOUNT \
                      --enable CONFIG_KSU_SUSFS_SUS_KSTAT \
                      --disable CONFIG_KSU_SUSFS_SUS_OVERLAYFS \
                      --enable CONFIG_KSU_SUSFS_TRY_UMOUNT \
                      --enable CONFIG_KSU_SUSFS_SPOOFER \
                      --enable CONFIG_KSU_SUSFS_OPEN_REDIRECT \
                      --disable CONFIG_KSU_SUSFS_SUS_SU
                  fi

                  # NTSYNC is always enabled
                  ./scripts/config --file arch/arm64/configs/custom.config --enable CONFIG_NTSYNC

                  # BBRv3 — enabled unless disabled via input
                  if [[ "${{ github.event.inputs.enable_bbrv3 }}" != "false" ]]; then
                    echo "⚙️ Enabling BBRv3..."
                    ./scripts/config --file arch/arm64/configs/custom.config \
                      --enable CONFIG_TCP_CONG_ADVANCED \
                      --enable CONFIG_TCP_CONG_BBR \
                      --enable CONFIG_TCP_CONG_BBR3 \
                      --enable CONFIG_TCP_CONG_CUBIC \
                      --enable CONFIG_DEFAULT_CUBIC \
                      --enable CONFIG_NET_SCH_FQ
                  fi

                  if [[ "${{ matrix.zeromount }}" == "true" && "${{ github.event.inputs.enable_zeromount }}" != "false" ]]; then
                    echo "⚙️ Enabling Zeromount..."
                    ./scripts/config --file arch/arm64/configs/custom.config --enable CONFIG_ZEROMOUNT
                  fi

                  # TMPFS XATTR is always enabled (mountify)
                  ./scripts/config --file arch/arm64/configs/custom.config --enable CONFIG_TMPFS_XATTR

                  # BBG is always enabled
                  ./scripts/config --file arch/arm64/configs/custom.config --enable CONFIG_BBG

                  echo "✅ Build configuration ready"
                  echo "--- custom.config ---"
                  cat arch/arm64/configs/custom.config

            - name: Build kernel
              run: |
                  set -euo pipefail
                  cd "$GITHUB_WORKSPACE"
                  export PATH="${{ env.TOOLCHAIN_DIR }}/bin:$PATH"
                  export KBUILD_BUILD_HOST="Github-Actions"
                  export KBUILD_BUILD_USER="bizarre"
                  export PLATFORM_VERSION=16
                  export TARGET_SOC=s5e8835

                  MAKE_PARAMS="-j$(nproc --all) ARCH=arm64 KBUILD_OUTPUT=$GITHUB_WORKSPACE/out CC=clang LLVM=1 LLVM_IAS=1 CROSS_COMPILE=aarch64-linux-gnu- CROSS_COMPILE_ARM32=arm-linux-gnueabi-"

                  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
                  echo "🏗️  Starting Bizarre Kernel build"
                  echo "   Variant    : ${{ matrix.variant }}"
                  echo "   Threads    : $(nproc --all)"
                  echo "   Clang      : $(clang --version | head -1)"
                  echo "   Output     : $GITHUB_WORKSPACE/out"
                  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

                  echo "⚙️  Generating .config from bizarre_defconfig + custom.config..."
                  make $MAKE_PARAMS bizarre_defconfig custom.config

                  echo ""
                  echo "🔨 Compiling kernel & DTBs..."
                  BUILD_START=$(date +%s)
                  make $MAKE_PARAMS && make $MAKE_PARAMS dtbs
                  BUILD_END=$(date +%s)
                  BUILD_ELAPSED=$(( BUILD_END - BUILD_START ))

                  echo ""
                  echo "✅ Kernel compiled in $(( BUILD_ELAPSED / 60 ))m $(( BUILD_ELAPSED % 60 ))s"
                  echo "BUILD_ELAPSED=$BUILD_ELAPSED" >> "$GITHUB_ENV"

            - name: Verify kernel build
              run: |
                  IMAGE_PATH="$GITHUB_WORKSPACE/out/arch/arm64/boot/Image"
                  if [[ ! -f "$IMAGE_PATH" ]]; then
                    echo "❌ Kernel Image not found — build failed!"
                    exit 1
                  fi
                  IMAGE_SIZE=$(du -sh "$IMAGE_PATH" | cut -f1)
                  echo "✅ Kernel Image found — Size: $IMAGE_SIZE"
                  echo "IMAGE_SIZE=$IMAGE_SIZE" >> "$GITHUB_ENV"

            - name: Collect DTBOs per device
              run: |
                  set -euo pipefail
                  OUT=$GITHUB_WORKSPACE/out/arch/arm64/boot

                  echo "⬇️  Downloading mkdtboimg.py tool from AOSP..."
                  python3 -c "import urllib.request, base64; url = 'https://android.googlesource.com/platform/system/libufdt/+/refs/heads/main/utils/src/mkdtboimg.py?format=TEXT'; data = urllib.request.urlopen(url).read(); open('mkdtboimg.py', 'wb').write(base64.b64decode(data))"

                  echo "📦 Packing M35x DTBOs..."
                  python3 mkdtboimg.py create \
                    dtbo-m35x.img \
                    $(ls $OUT/dts/samsung/m35x/*.dtbo | sort)

                  echo "📦 Packing A35x DTBOs..."
                  python3 mkdtboimg.py create \
                    dtbo-a35x.img \
                    $(ls $OUT/dts/samsung/a35x/*.dtbo | sort)

                  echo "✅ DTBOs ready"

            - name: Create flashable zip
              run: |
                  set -euo pipefail
                  KERNEL_NAME_RAW="${{ env.BUILD_LOCALVERSION }}"
                  KERNEL_NAME_RAW="${KERNEL_NAME_RAW#-}"
                  ZIP_NAME="${KERNEL_NAME_RAW}-${{ env.DATE }}.zip"

                  echo "📦 Packaging into $ZIP_NAME..."
                  cp "$GITHUB_WORKSPACE/out/arch/arm64/boot/Image" "$HOME/AnyKernel3/"
                  cp dtbo-m35x.img "$HOME/AnyKernel3/"
                  cp dtbo-a35x.img "$HOME/AnyKernel3/"

                  cd "$HOME/AnyKernel3"
                  7z a "$ZIP_NAME" . -xr'!*.git*' | tail -1

                  ZIP_SIZE=$(du -sh "$ZIP_NAME" | cut -f1)
                  echo "✅ Zip created — Size: $ZIP_SIZE"
                  echo "ZIP_NAME=$ZIP_NAME" >> "$GITHUB_ENV"
                  echo "ZIP_SIZE=$ZIP_SIZE" >> "$GITHUB_ENV"

            - name: Upload kernel artifact
              uses: actions/upload-artifact@v4
              with:
                  name: Bizarre-Kernel-${{ matrix.variant }}-${{ env.DATE }}
                  path: /home/runner/AnyKernel3/*.zip
                  if-no-files-found: error
                  retention-days: 7

    publish-release:
        name: Publish GitHub Release
        needs: [build]
        runs-on: ubuntu-latest
        if: >-
            !cancelled()
            && needs.build.result == 'success'
        steps:
            - name: Set date
              id: date
              run: echo "DATE=$(date +'%Y-%m-%d')" >> "$GITHUB_OUTPUT"

            - name: Download kernel artifacts
              uses: actions/download-artifact@v4
              with:
                  pattern: Bizarre-Kernel-*
                  path: /home/runner/release
                  merge-multiple: true

            - name: Publish to GitHub Releases
              uses: softprops/action-gh-release@v2.6.1
              with:
                  files: /home/runner/release/*.zip
                  name: Bizarre Kernel - ${{ steps.date.outputs.DATE }}
                  tag_name: ${{ steps.date.outputs.DATE }}
                  prerelease: ${{ github.event.inputs.prerelease == 'true' }}
                  overwrite_files: true
                  body: |
                      ### Bizarre Kernel for Galaxy M35 + A35 (Exynos 1380 / s5e8835)

                      ### Features
                      - KernelSU-Next: https://github.com/rifsxd/KernelSU-Next (matrix variants)
                      - KernelSU (tiann stable): https://github.com/tiann/KernelSU (matrix variants)
                      - SUSFS: https://gitlab.com/simonpunk/susfs4ksu
                      - BBRv3 network TCP congestion control
                      - NTSYNC support
                      - Unified kernel for both SM-M356B and SM-A356B
                      - Android 16 / One UI 7

                      ### Flashing Instructions
                      1. Flash the zip via KernelSU Manager or any recovery
                      2. Device is automatically detected — correct DTBO is flashed
                      3. Reboot

                      ### Build information
                      - **Kernel:** android13-5.15 (LTS ${{ steps.date.outputs.DATE }})
                      - **Clang:** r450784d
                      - **Target SoC:** Exynos 1380 (s5e8835)
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    notify-release:
        name: Send Release Notification
        runs-on: ubuntu-latest
        needs: [publish-release]
        if: github.event.inputs.notifications == 'true' && needs.publish-release.result == 'success'
        steps:
            - name: Set date
              id: date
              run: echo "DATE=$(date +'%Y-%m-%d')" >> "$GITHUB_OUTPUT"

            - name: Send Telegram Release Notification
              run: |
                  curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
                    -F chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
                    -F message_thread_id="${{ secrets.TELEGRAM_MESSAGE_THREAD_ID }}" \
                    -F parse_mode="Markdown" \
                    -F disable_web_page_preview=true \
                    -F text="$(printf '%s\n' \
                      '🎉 *New Bizarre Kernel Release*' \
                      '━━━━━━━━━━━━━━━━━━━━━━' \
                      '🗓️ *Date:* \`${{ steps.date.outputs.DATE }}\`' \
                      '📱 *Devices:* \`Galaxy M35 + A35 (s5e8835)\`' \
                      '━━━━━━━━━━━━━━━━━━━━━━' \
                      '📥 [Download from GitHub Releases](https://github.com/${{ github.repository }}/releases/tag/${{ steps.date.outputs.DATE }})')"
